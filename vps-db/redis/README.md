# Redis Template

This folder contains a production-oriented Redis template.

## Quick start

```bash
cp .env.example .env
cp redis.conf.example redis.conf
nano .env
nano redis.conf
docker compose up -d
```
