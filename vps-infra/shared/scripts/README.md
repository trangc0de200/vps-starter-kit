# Shared Scripts

Thư mục này dành cho các script dùng chung như:

- backup_all.sh
- healthcheck_all.sh
- cleanup.sh
- rotate_logs.sh

Sau khi chạy `install.sh`, các helper script cơ bản sẽ được tạo trực tiếp trên VPS trong:

```text
/opt/vps/vps-infra/shared/scripts/
```
