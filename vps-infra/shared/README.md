# Shared Infrastructure Utilities

This folder contains shared operational helpers intended to be used across the whole VPS.

Included sections:
- `scripts/` for operational shell helpers
- `cron/` for cron examples
- `bin/` for future small CLI wrappers
- `templates/` for future reusable file templates
