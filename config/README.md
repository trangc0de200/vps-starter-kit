# Platform Configuration

This folder contains centralized platform-level configuration.

## Primary File
- `platform.yml`

## Purpose
This config acts as a readable registry for platform defaults, network names, project list, service list, and plugin list.
